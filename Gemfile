source 'https://rubygems.org'

ruby '2.3.3'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.0'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'
# Use HAML for markup in views
gem 'haml-rails'
# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Functionality core
gem 'math_engine'

# Sorta pretty UI
gem "bootstrap-sass"
gem "font-awesome-rails"

# Use jquery as the JavaScript library
gem 'jquery-rails'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
# gem 'sdoc', '~> 0.4.0', group: :doc

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Tweaks for working on 12-factor platforms, such as Heroku
gem 'rails_12factor', group: :production

# Use Unicorn as the app server
gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

group :development, :test do
  # Replace IRB with Pry on Rails console
  gem 'pry-rails'
  # ...and fancy syntax highlighting
  gem 'pry-coolline'

  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug'
  gem 'pry-byebug'

  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'

  # Nice error pages
  gem 'better_errors'

  # REPL anywhere in stacktrace
  gem 'binding_of_caller'

  # Rails Panel integration for Chromium-based browsers
  gem 'meta_request'

  # For writing specs
  gem 'rspec-rails', '~> 3.0'

  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring', '~> 1.7.1'

  # Debug actual CoffeeScript, not JS
  gem 'coffee-rails-source-maps'
end
